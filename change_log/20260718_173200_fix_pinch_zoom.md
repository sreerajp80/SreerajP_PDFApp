# Change Log - Fix Pinch-Zoom Issue After Menu Actions

**Date:** 2026-07-18
**Implements plan:** [plans/20260718_170100_fix_pinch_zoom.md](file:///l:/Android/SreerajP_PDFApp/plans/20260718_170100_fix_pinch_zoom.md)

## Summary of Changes

### Viewer Screen
- Modified [viewer_screen.dart](file:///l:/Android/SreerajP_PDFApp/lib/features/viewer/presentation/viewer_screen.dart):
  - Declared a stable `final _viewerKey = GlobalKey();` inside `_ViewerScreenState`.
  - Passed `key: _viewerKey` to `PinchZoomWrapper` inside `_buildBody`. This ensures that the viewer element subtree remains preserved and reparented rather than disposed and recreated when route changes or top notice widgets are conditionally added/removed.
  - Added `_identityFilter` representing an identity color matrix.
  - Always returned a `ColorFiltered` widget in `_buildBody`, using `_invertFilter` if `_invert` is true and `_identityFilter` if false. This prevents depth transitions and guarantees a stable layout hierarchy.

### Pinch Zoom Wrapper
- Reverted temporary debug prints from [pinch_zoom_wrapper.dart](file:///l:/Android/SreerajP_PDFApp/lib/features/viewer/presentation/widgets/pinch_zoom_wrapper.dart).

## Verification
- Verified that all unit and widget tests pass: `All tests passed! (307 passed)`
- Run `flutter analyze` completed with no static analysis errors.
