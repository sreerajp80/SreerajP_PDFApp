# Change log — Create a minimal CLAUDE.md with split subfiles

**Date:** 2026-07-14 13:43 (local time)
**Implements plan:** [../plans/20260714_133333_create-claude-md.md](../plans/20260714_133333_create-claude-md.md)

## What was changed

Created the project rules file and its two detail subfiles, mirroring the sibling project
`SreerajP_TextApp` so all projects follow the same structure.

### Files created

- `CLAUDE.md` (project root) — minimal project-rules entry point with the same 8-section
  layout as `SreerajP_TextApp`:
  1. What this app is (points to `doc/PDF-Idea.md`)
  2. Tech stack (fixed) — Flutter 3.41.9+, Dart 3.11.5+, minSdk 26, Material 3, core libraries
  3. Hard rules — open source only, offline-first, scoped storage (SAF), never crash on bad
     input, copy-on-write, never a dead button, OCR out of scope
  4. Security rules (points to `doc/security-rules.md`)
  5. Where things live
  6. Guidelines (points to `doc/GUIDELINES_MANIFEST.md`)
  7. Workflow rules (points to `doc/workflow-rules.md`)
  8. Communication rules — simple English
- `doc/workflow-rules.md` — full plan-before-change, approval-gate, and log-after-change
  rules (the global workflow rules).
- `doc/security-rules.md` — PDF-specific security rules: untrusted input, offline, copy-on-write,
  local signature verification (Bouncy Castle + `CertPathValidator`, AATL/EUTL trust lists),
  no logging of secrets, content-fingerprint file identity, scoped storage.

### Files referenced, not modified

- `doc/PDF-Idea.md` and `doc/GUIDELINES_MANIFEST.md` — linked from `CLAUDE.md`, left unchanged.

## Notes

- Content was summarised and cross-linked, not copy-pasted from `PDF-Idea.md`.
- Structure matches `SreerajP_TextApp` per the user's requirement that all projects follow the
  same structure and guidelines. The only difference is this project uses the `doc/` folder
  (TextApp uses `docs/`), matching each project's existing layout.
