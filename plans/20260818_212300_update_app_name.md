# Update App Name to SreerajP PDF App

**Status:** completed

## Issue

The user requested that the app name should be **SreerajP PDF App**. While `assets/config/app_config.json` and `android/app/build.gradle.kts` already use "SreerajP PDF App", several places across the Flutter codebase (flavor configuration, English localizations, main logging, documentation, and tests) still refer to "PDF App".

## Files to change

- `lib/app/config/app_flavor_config.dart`: Update `appName` getter to return `'SreerajP PDF App Dev'` for dev and `'SreerajP PDF App'` for prod.
- `lib/l10n/app_en.arb`: Update `appTitle` and `homeTitle` to `"SreerajP PDF App"`.
- `lib/l10n/app_localizations_en.dart`: Regenerate or update localization getter values for `appTitle` and `homeTitle`.
- `lib/l10n/app_localizations.dart`: Regenerate or update localization comments/declarations.
- `lib/main.dart`: Update log message from `'Starting PDF App.'` to `'Starting SreerajP PDF App.'`.
- `test/app/config/app_flavor_config_test.dart`: Update expected test assertion value to `'SreerajP PDF App'`.
- `README.md`: Update flavor descriptions to reference "SreerajP PDF App" and "SreerajP PDF App Dev".

## Plan for the fix

1. Update `lib/app/config/app_flavor_config.dart` so `appName` returns `'SreerajP PDF App Dev'` or `'SreerajP PDF App'`.
2. Update `lib/l10n/app_en.arb` setting `appTitle` and `homeTitle` to `"SreerajP PDF App"`.
3. Run `flutter gen-l10n` (or update localization files) to sync localization Dart files with ARB.
4. Update `lib/main.dart` startup log string.
5. Update `test/app/config/app_flavor_config_test.dart` to assert `'SreerajP PDF App'`.
6. Update flavor docstrings in `README.md`.

## Verification

- Run all unit and widget tests: `flutter test`
- Ensure all tests pass with zero failures.
